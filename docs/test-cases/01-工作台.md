# 01 · 工作台 Dashboard 测试用例

> 编写依据：[PRD §3 场景1 / §4.2 模块1](../FDE工作台产品需求文档.md) + [dashboard.html](../prototype/pages/dashboard.html) + [02-后端详细设计.md §2.3](../detail-design/02-后端详细设计.md)
> 编写规范：见 [00-通用规范.md](./00-通用规范.md)

| 项目 | 信息 |
|------|------|
| 模块 | 工作台 |
| 模块代号 | DASH |
| 用例总数 | 30（功能 18 + UI 8 + 接口 4） |
| 优先级分布 | P0:6 / P1:13 / P2:9 / P3:2 |
| 是否含 Copilot | 否（v1.1 规划「工作台助手 W」） |

---

## 一、功能用例（F · 18 条）

### TC-DASH-F-001 工作台首屏正常加载（主流程）

| 字段 | 值 |
|------|-----|
| 模块 | 工作台 |
| 优先级 | P0 |
| 类型 | 功能 |
| 前置条件 | 1. 已用 fde_p6_01 登录；2. 当前用户有种子数据（10 任务/2 项目/3 客户） |

**测试步骤**：
1. 浏览器地址栏输入工作台 URL `/dashboard`
2. 等待页面完全加载

**预期结果**：
1. 页面 URL 显示 `/dashboard`，左侧导航高亮"工作台"
2. 欢迎横幅 + 4 张统计卡 + 最近任务 + 最近项目 + 通知 + 关键事件流 6 大区块全部可见，无空白/报错

---

### TC-DASH-F-002 欢迎横幅显示当日聚焦数据

| 字段 | 值 |
|------|-----|
| 模块 | 工作台 |
| 优先级 | P0 |
| 类型 | 功能 |
| 前置条件 | 1. fde_p6_01 已登录；2. 数据库存在 3 项 P0 任务、2 个客户会议、1 份待提交周报 |

**测试步骤**：
1. 进入工作台
2. 查看欢迎横幅文案

**预期结果**：
1. 横幅顶部显示问候语（"早上好/下午好/晚上好"根据时间动态切换）+ 用户名"吾明"
2. 文案精确显示"今天有 **3 项 P0 任务**、**2 个客户会议**、**1 份周报**待提交"，数字加粗

---

### TC-DASH-F-003 "帮我准备晨会"按钮触发摘要生成

| 字段 | 值 |
|------|-----|
| 模块 | 工作台 |
| 优先级 | P1 |
| 类型 | 功能 |
| 前置条件 | fde_p6_01 已登录工作台 |

**测试步骤**：
1. 点击欢迎横幅右下角"帮我准备晨会"按钮
2. 等待响应

**预期结果**：
1. 按钮变为 loading 态，禁用重复点击
2. 3 秒内弹出晨会摘要面板，包含"昨日完成 / 今日计划 / 阻塞"3 段内容，每段不少于 1 条

---

### TC-DASH-F-004 4 张统计卡数字正确

| 字段 | 值 |
|------|-----|
| 模块 | 工作台 |
| 优先级 | P0 |
| 类型 | 功能 |
| 前置条件 | fde_p6_01 已登录，种子数据 10 任务 / 2 项目 / 3 客户 / 5 文件 |

**测试步骤**：
1. 进入工作台
2. 查看 4 张统计卡（任务总数 / 项目总数 / 客户总数 / 文件总数）

**预期结果**：
1. 4 张卡片横向排布，间距均匀
2. 数字精确显示 "10 / 2 / 3 / 5"，与种子数据一致

---

### TC-DASH-F-005 点击统计卡跳转对应模块

| 字段 | 值 |
|------|-----|
| 模块 | 工作台 |
| 优先级 | P1 |
| 类型 | 功能 |
| 前置条件 | fde_p6_01 已登录工作台 |

**测试步骤**：
1. 依次点击"任务总数"卡、"项目总数"卡、"客户总数"卡、"文件总数"卡

**预期结果**：
1. 4 次点击分别跳转 `/tasks`、`/projects`、`/customers`、`/files`，URL 切换 < 200ms

---

### TC-DASH-F-006 最近任务列表默认显示 10 条

| 字段 | 值 |
|------|-----|
| 模块 | 工作台 |
| 优先级 | P1 |
| 类型 | 功能 |
| 前置条件 | fde_p6_01 已登录，种子数据存在 ≥ 10 任务 |

**测试步骤**：
1. 进入工作台，下滚到"最近任务"区块
2. 查看任务条数

**预期结果**：
1. 区块标题"最近任务"右侧有"查看全部 →"链接
2. 列表精确显示 10 条任务，按 `gmt_modified DESC` 排序

---

### TC-DASH-F-007 最近任务条目元素完整

| 字段 | 值 |
|------|-----|
| 模块 | 工作台 |
| 优先级 | P1 |
| 类型 | 功能 |
| 前置条件 | 工作台"最近任务"已渲染 |

**测试步骤**：
1. 检查任务条目内的元素

**预期结果**：
1. 每条任务包含：优先级 dot（P0红/P1橙/P2蓝/P3灰）+ 标题 + 状态 tag + 截止日期 + 关联项目名（若有）

---

### TC-DASH-F-008 点击最近任务跳转任务详情

| 字段 | 值 |
|------|-----|
| 模块 | 工作台 |
| 优先级 | P1 |
| 类型 | 功能 |
| 前置条件 | 工作台"最近任务"已渲染 |

**测试步骤**：
1. 点击列表中第一条任务的标题

**预期结果**：
1. 跳转至 `/tasks/{id}`，任务详情页打开，URL 含正确 id

---

### TC-DASH-F-009 最近项目列表默认显示 5 条

| 字段 | 值 |
|------|-----|
| 模块 | 工作台 |
| 优先级 | P1 |
| 类型 | 功能 |
| 前置条件 | fde_p6_01 已登录，种子数据存在 2 项目 |

**测试步骤**：
1. 进入工作台，下滚到"最近项目"区块

**预期结果**：
1. 项目数 ≤ 5 时全部显示（本例 2 条），无 "查看更多" 提示
2. 每条项目显示：项目名 + 客户等级 tag + 阶段 + 健康度环 mini + 进度百分比

---

### TC-DASH-F-010 点击最近项目跳转项目详情

| 字段 | 值 |
|------|-----|
| 模块 | 工作台 |
| 优先级 | P1 |
| 类型 | 功能 |
| 前置条件 | 工作台"最近项目"已渲染 |

**测试步骤**：
1. 点击"阿里云A数据中台"项目卡

**预期结果**：
1. 跳转至 `/projects/10001`，项目详情打开

---

### TC-DASH-F-011 通知列表分页与未读标识

| 字段 | 值 |
|------|-----|
| 模块 | 工作台 |
| 优先级 | P2 |
| 类型 | 功能 |
| 前置条件 | fde_p6_01 已登录，存在 25 条通知（10 未读 + 15 已读） |

**测试步骤**：
1. 进入工作台，定位"通知"区块
2. 滚动到通知列表底部，点击"加载更多"

**预期结果**：
1. 第一页显示 10 条通知，未读条目左侧有蓝点（•）
2. 点击后追加显示第 11-20 条

---

### TC-DASH-F-012 点击通知标记已读

| 字段 | 值 |
|------|-----|
| 模块 | 工作台 |
| 优先级 | P2 |
| 类型 | 功能 |
| 前置条件 | 通知列表存在 ≥ 1 条未读通知 |

**测试步骤**：
1. 点击一条未读通知

**预期结果**：
1. 蓝点消失，通知背景从浅蓝变白；如关联具体对象则同时跳转该对象详情

---

### TC-DASH-F-013 关键事件流默认显示近 7 天

| 字段 | 值 |
|------|-----|
| 模块 | 工作台 |
| 优先级 | P2 |
| 类型 | 功能 |
| 前置条件 | fde_p6_01 已登录，最近 7 天有 5 个关键事件，第 8 天有 1 个事件 |

**测试步骤**：
1. 进入工作台，查看"关键事件流"

**预期结果**：
1. 列表显示 5 条事件（第 8 天事件不展示），按事件时间倒序
2. 每条事件含：图标（任务/项目/客户/文件分类色）+ 描述 + 相对时间（如"3 小时前"）

---

### TC-DASH-F-014 切换事件流时间范围

| 字段 | 值 |
|------|-----|
| 模块 | 工作台 |
| 优先级 | P2 |
| 类型 | 功能 |
| 前置条件 | 工作台已加载 |

**测试步骤**：
1. 在"关键事件流"标题右侧切换器选择"近 30 天"

**预期结果**：
1. 列表刷新，显示 30 天内全部事件，加载耗时 < 500ms

---

### TC-DASH-F-015 新用户空数据态友好提示

| 字段 | 值 |
|------|-----|
| 模块 | 工作台 |
| 优先级 | P2 |
| 类型 | 功能 |
| 前置条件 | 使用全新账号 fde_p5_01 登录（无任何任务/项目/客户/文件） |

**测试步骤**：
1. 进入工作台

**预期结果**：
1. 4 张统计卡显示 "0"，最近任务/项目/通知/事件区块显示空状态插画 + "暂无数据，去[创建]" 引导按钮

---

### TC-DASH-F-016 退出登录跳转登录页

| 字段 | 值 |
|------|-----|
| 模块 | 工作台 |
| 优先级 | P1 |
| 类型 | 功能 |
| 前置条件 | fde_p6_01 已登录工作台 |

**测试步骤**：
1. 点击右上角头像，下拉菜单选"退出登录"

**预期结果**：
1. 跳转至 `/login`，本地 token 被清除，再次访问 `/dashboard` 自动跳登录页

---

### TC-DASH-F-017 Token 过期工作台静默刷新

| 字段 | 值 |
|------|-----|
| 模块 | 工作台 |
| 优先级 | P1 |
| 类型 | 功能 |
| 前置条件 | fde_p6_01 已登录，access_token 即将过期但 refresh_token 有效 |

**测试步骤**：
1. 等待 access_token 过期
2. 在工作台进行任意拉数据操作（如刷新关键事件流）

**预期结果**：
1. 不弹登录框，前端自动用 refresh_token 换 token，操作成功完成

---

### TC-DASH-F-018 横幅按角色不同问候语

| 字段 | 值 |
|------|-----|
| 模块 | 工作台 |
| 优先级 | P3 |
| 类型 | 功能 |
| 前置条件 | 同账号在 9:00 / 14:00 / 21:00 三个时段访问 |

**测试步骤**：
1. 9:00 访问工作台，记录问候语
2. 14:00 访问工作台
3. 21:00 访问工作台

**预期结果**：
1. 9:00 显示"早上好"
2. 14:00 显示"下午好"
3. 21:00 显示"晚上好"

---

## 二、UI/UX 用例（U · 8 条）

### TC-DASH-U-001 三栏布局比例正确（1440 分辨率）

| 字段 | 值 |
|------|-----|
| 模块 | 工作台 |
| 优先级 | P0 |
| 类型 | UI |
| 前置条件 | 浏览器分辨率 1440×900，fde_p6_01 已登录 |

**测试步骤**：
1. 进入工作台，使用开发者工具测量布局

**预期结果**：
1. 左侧 Nav 220px + 中间 Main 自适应 + 右侧 Copilot 隐藏（工作台无 Copilot），实际 Main 宽度 = 1440 - 220 = 1220px

---

### TC-DASH-U-002 4 张统计卡渐变色与图标

| 字段 | 值 |
|------|-----|
| 模块 | 工作台 |
| 优先级 | P1 |
| 类型 | UI |
| 前置条件 | 工作台已加载 |

**测试步骤**：
1. 检查 4 张统计卡视觉

**预期结果**：
1. 4 张卡左侧分别有彩色图标（任务蓝/项目紫/客户橙/文件绿），顶部有 3px 同色渐变条；hover 上浮 2px 投影变深

---

### TC-DASH-U-003 优先级 dot 颜色规范

| 字段 | 值 |
|------|-----|
| 模块 | 工作台 |
| 优先级 | P1 |
| 类型 | UI |
| 前置条件 | "最近任务"中存在 P0/P1/P2/P3 四种优先级任务 |

**测试步骤**：
1. 检查每条任务前的 dot 颜色

**预期结果**：
1. P0 = #FF4D4F（红）/ P1 = #FA8C16（橙）/ P2 = #1677FF（蓝）/ P3 = #8C8C8C（灰），dot 直径 6px 带同色光晕

---

### TC-DASH-U-004 任务状态 tag 配色

| 字段 | 值 |
|------|-----|
| 模块 | 工作台 |
| 优先级 | P2 |
| 类型 | UI |
| 前置条件 | 任务列表存在 5 种状态 |

**测试步骤**：
1. 对照 todo / in_progress / review / done / blocked 5 种状态 tag

**预期结果**：
1. 待办灰 / 进行中蓝 / 评审紫 / 完成绿 / 阻塞红，圆角 4px，文字与背景对比度 ≥ 4.5:1

---

### TC-DASH-U-005 健康度环渐变色（mini）

| 字段 | 值 |
|------|-----|
| 模块 | 工作台 |
| 优先级 | P2 |
| 类型 | UI |
| 前置条件 | "最近项目"中存在健康度 90/65/40 三种项目 |

**测试步骤**：
1. 检查项目卡上的 mini 健康度环（直径 28px）

**预期结果**：
1. 90 显示绿色满环 / 65 显示黄色 234° 环 / 40 显示红色 144° 环，使用 `conic-gradient` 实现

---

### TC-DASH-U-006 通知未读蓝点呼吸效果

| 字段 | 值 |
|------|-----|
| 模块 | 工作台 |
| 优先级 | P3 |
| 类型 | UI |
| 前置条件 | 存在 ≥ 1 条未读通知 |

**测试步骤**：
1. 观察未读蓝点动画

**预期结果**：
1. 蓝点透明度 0.6 → 1.0 循环渐变，周期 1.5s，无卡顿

---

### TC-DASH-U-007 1280 分辨率适配

| 字段 | 值 |
|------|-----|
| 模块 | 工作台 |
| 优先级 | P1 |
| 类型 | UI |
| 前置条件 | 浏览器分辨率切到 1280×800 |

**测试步骤**：
1. 刷新工作台

**预期结果**：
1. 4 张统计卡仍横向 4 列不换行，最近任务/项目区块上下平铺，无横向滚动条

---

### TC-DASH-U-008 1024 以下不支持提示

| 字段 | 值 |
|------|-----|
| 模块 | 工作台 |
| 优先级 | P3 |
| 类型 | UI |
| 前置条件 | 浏览器宽度 < 1024 |

**测试步骤**：
1. 访问工作台

**预期结果**：
1. 居中显示提示页"请使用更大分辨率（≥ 1280px）打开 FDE 工作台"

---

## 三、接口用例（I · 4 条）

### TC-DASH-I-001 GET /dashboard/summary 正常返回

| 字段 | 值 |
|------|-----|
| 模块 | 工作台 |
| 优先级 | P0 |
| 类型 | 接口 |
| 前置条件 | fde_p6_01 token 有效 |

**测试步骤**：
1. 发起 `GET /api/v1/dashboard/summary`，Header 携带 `Authorization: Bearer {token}`

**预期结果**：
1. HTTP 200，响应体 `{code:0, data:{taskCount:10, projectCount:2, customerCount:3, fileCount:5}, traceId:"xxx"}`

---

### TC-DASH-I-002 GET /dashboard/recent-tasks 默认 limit=10

| 字段 | 值 |
|------|-----|
| 模块 | 工作台 |
| 优先级 | P1 |
| 类型 | 接口 |
| 前置条件 | fde_p6_01 token 有效，存在 ≥ 10 任务 |

**测试步骤**：
1. 发起 `GET /api/v1/dashboard/recent-tasks`（不传 limit 参数）
2. 发起 `GET /api/v1/dashboard/recent-tasks?limit=5`

**预期结果**：
1. 响应 `data.length == 10`
2. 响应 `data.length == 5`

---

### TC-DASH-I-003 未登录访问返回 401

| 字段 | 值 |
|------|-----|
| 模块 | 工作台 |
| 优先级 | P0 |
| 类型 | 接口 |
| 前置条件 | 不携带 Authorization Header |

**测试步骤**：
1. 发起 `GET /api/v1/dashboard/summary`，不带 token

**预期结果**：
1. HTTP 401，响应体 `{code:2001, message:"未登录或登录已过期"}`

---

### TC-DASH-I-004 GET /dashboard/key-events?days 边界

| 字段 | 值 |
|------|-----|
| 模块 | 工作台 |
| 优先级 | P2 |
| 类型 | 接口 |
| 前置条件 | fde_p6_01 token 有效 |

**测试步骤**：
1. 请求 `?days=7`
2. 请求 `?days=0`（边界）
3. 请求 `?days=-1`（非法）
4. 请求 `?days=365`（最大）

**预期结果**：
1. 200，返回 7 天数据
2. 200，返回空数组
3. 422 BIZ_INVALID_PARAM
4. 200，返回 365 天数据；> 365 应返回 422

---

**文档结束** · 共 30 条用例（功能 18 + UI 8 + 接口 4）
