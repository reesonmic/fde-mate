# 02 · 任务中心 + T 任务助手 测试用例

> 编写依据：[PRD §3 场景2 / §5.2](../FDE工作台产品需求文档.md) + [tasks.html](../prototype/pages/tasks.html) + [02-后端详细设计.md §2.4](../detail-design/02-后端详细设计.md)
> 编写规范：见 [00-通用规范.md](./00-通用规范.md)

| 项目 | 信息 |
|------|------|
| 模块 | 任务中心 + T 任务助手 |
| 模块代号 | TASK |
| 用例总数 | 55（功能 30 + UI 12 + 接口 13） |
| 优先级分布 | P0:11 / P1:25 / P2:14 / P3:5 |
| 关联 Copilot | T 任务助手（蓝渐变 #1677FF → #0050B3） |

---

## 一、功能用例（F · 30 条）

### TC-TASK-F-001 任务列表正常加载（主流程）

| 字段 | 值 |
|------|-----|
| 模块 | 任务中心 |
| 优先级 | P0 |
| 类型 | 功能 |
| 前置条件 | 1. fde_p6_01 已登录；2. 数据库存在 10 任务（属于该用户） |

**测试步骤**：
1. 点击左侧导航"任务中心"
2. 等待页面加载

**预期结果**：
1. URL 切到 `/tasks`，导航高亮
2. 列表显示 10 条任务，按 `gmt_create DESC` 排序，每页 20 条，分页器显示"共 10 条"

---

### TC-TASK-F-002 关键词搜索任务

| 字段 | 值 |
|------|-----|
| 模块 | 任务中心 |
| 优先级 | P1 |
| 类型 | 功能 |
| 前置条件 | 任务列表存在标题含"POC"的任务 2 条，其他 8 条 |

**测试步骤**：
1. 在搜索框输入"POC"
2. 按回车

**预期结果**：
1. 搜索框出现 "POC" 文字
2. 列表刷新，仅剩 2 条匹配任务，标题中"POC"高亮黄底

---

### TC-TASK-F-003 状态多选筛选

| 字段 | 值 |
|------|-----|
| 模块 | 任务中心 |
| 优先级 | P1 |
| 类型 | 功能 |
| 前置条件 | 列表存在 5 种状态各 2 条 |

**测试步骤**：
1. 点击"状态"筛选下拉
2. 勾选 `todo` 与 `in_progress`，确定

**预期结果**：
1. 下拉打开，5 个 checkbox 默认未选
2. 列表刷新仅剩 4 条，筛选标签栏显示 "状态: 待办,进行中"

---

### TC-TASK-F-004 责任人筛选

| 字段 | 值 |
|------|-----|
| 模块 | 任务中心 |
| 优先级 | P1 |
| 类型 | 功能 |
| 前置条件 | fde_p7_01 登录（可看团队任务），列表中存在 fde_p6_01/fde_p5_01 各 5 任务 |

**测试步骤**：
1. 点击"责任人"筛选，选 fde_p6_01

**预期结果**：
1. 列表刷新仅剩 fde_p6_01 任务 5 条

---

### TC-TASK-F-005 项目筛选

| 字段 | 值 |
|------|-----|
| 模块 | 任务中心 |
| 优先级 | P1 |
| 类型 | 功能 |
| 前置条件 | fde_p6_01 登录，"阿里云A数据中台"项目下有 6 任务，"银行B"下有 4 任务 |

**测试步骤**：
1. 点击"项目"筛选，选"阿里云A数据中台"

**预期结果**：
1. 列表仅剩 6 条该项目任务

---

### TC-TASK-F-006 优先级多选筛选

| 字段 | 值 |
|------|-----|
| 模块 | 任务中心 |
| 优先级 | P1 |
| 类型 | 功能 |
| 前置条件 | 列表存在 P0×2 / P1×3 / P2×3 / P3×2 共 10 任务 |

**测试步骤**：
1. 点击"优先级"筛选，勾选 P0 + P1

**预期结果**：
1. 列表刷新仅剩 5 条（2 P0 + 3 P1）

---

### TC-TASK-F-007 多筛选条件组合

| 字段 | 值 |
|------|-----|
| 模块 | 任务中心 |
| 优先级 | P2 |
| 类型 | 功能 |
| 前置条件 | 同 F-001 |

**测试步骤**：
1. 同时设置：关键词"POC" + 状态"in_progress" + 优先级"P0"
2. 点击"清空筛选"

**预期结果**：
1. 列表为多条件交集结果，筛选标签栏并列显示 3 个 chip
2. 所有筛选清空，列表恢复全部 10 条

---

### TC-TASK-F-008 任务列表分页切换

| 字段 | 值 |
|------|-----|
| 模块 | 任务中心 |
| 优先级 | P2 |
| 类型 | 功能 |
| 前置条件 | 数据库存在 45 条任务（fde_p7_01 登录可全见） |

**测试步骤**：
1. 进入任务列表
2. 点击分页第 2 页
3. 切换每页大小为 50

**预期结果**：
1. 第 1 页显示 20 条
2. 第 2 页显示 20 条（21-40），分页器高亮"2"
3. 切换后单页显示 45 条全部

---

### TC-TASK-F-009 任务列表列排序

| 字段 | 值 |
|------|-----|
| 模块 | 任务中心 |
| 优先级 | P2 |
| 类型 | 功能 |
| 前置条件 | 任务列表已加载 |

**测试步骤**：
1. 点击"截止时间"列头
2. 再次点击

**预期结果**：
1. 列表按截止时间升序，列头出现 ↑ 图标
2. 列表按截止时间降序，列头变 ↓ 图标

---

### TC-TASK-F-010 创建任务（主流程）

| 字段 | 值 |
|------|-----|
| 模块 | 任务中心 |
| 优先级 | P0 |
| 类型 | 功能 |
| 前置条件 | fde_p6_01 已登录任务页 |

**测试步骤**：
1. 点击右上角"+ 新建任务"
2. 输入标题"完成客户A的POC方案"，优先级 P0，截止 = 明天，关联项目"阿里云A数据中台"
3. 点击"保存"

**预期结果**：
1. 弹窗打开，必填项标"*"
2. 表单字段全部正确显示
3. 弹窗关闭，列表顶部出现新任务，状态=todo，HTTP 201

---

### TC-TASK-F-011 创建任务标题为空校验

| 字段 | 值 |
|------|-----|
| 模块 | 任务中心 |
| 优先级 | P1 |
| 类型 | 功能 |
| 前置条件 | 任务创建弹窗打开 |

**测试步骤**：
1. 标题留空，其他字段填好
2. 点击"保存"

**预期结果**：
1. 弹窗保留打开
2. 标题输入框红色边框 + 下方提示"标题不能为空"，无网络请求发起

---

### TC-TASK-F-012 创建任务标题超长边界

| 字段 | 值 |
|------|-----|
| 模块 | 任务中心 |
| 优先级 | P3 |
| 类型 | 功能 |
| 前置条件 | 任务创建弹窗打开 |

**测试步骤**：
1. 标题输入 200 字符
2. 提交
3. 标题输入 201 字符
4. 提交

**预期结果**：
1. 输入框接受
2. 创建成功
3. 字符计数器显示 "201/200" 红字
4. 提交报 422 BIZ_INVALID_PARAM "标题长度不能超过 200"

---

### TC-TASK-F-013 任务详情页打开

| 字段 | 值 |
|------|-----|
| 模块 | 任务中心 |
| 优先级 | P0 |
| 类型 | 功能 |
| 前置条件 | 任务列表存在 ≥ 1 条任务 |

**测试步骤**：
1. 点击任意任务标题

**预期结果**：
1. 跳转 `/tasks/{id}`，详情页显示：基本信息（标题/描述/优先级/状态/截止/创建者/责任人/项目）+ 评论区 + 操作历史 3 个 Tab

---

### TC-TASK-F-014 任务详情更新状态

| 字段 | 值 |
|------|-----|
| 模块 | 任务中心 |
| 优先级 | P0 |
| 类型 | 功能 |
| 前置条件 | 任务详情页打开，状态=todo |

**测试步骤**：
1. 点击状态 tag，下拉选 "in_progress"
2. 确认

**预期结果**：
1. 下拉显示 5 种状态
2. 状态变为"进行中"蓝 tag，操作历史新增一条记录"todo → in_progress"，列表回退后该任务状态同步

---

### TC-TASK-F-015 任务详情添加评论

| 字段 | 值 |
|------|-----|
| 模块 | 任务中心 |
| 优先级 | P1 |
| 类型 | 功能 |
| 前置条件 | 任务详情页打开 |

**测试步骤**：
1. 点击"评论"Tab
2. 在输入框输入"今天完成了 50%"，点击"发送"

**预期结果**：
1. Tab 切换到评论区
2. 评论列表新增一条，作者=fde_p6_01，时间="刚刚"，内容正确

---

### TC-TASK-F-016 任务详情查看历史

| 字段 | 值 |
|------|-----|
| 模块 | 任务中心 |
| 优先级 | P2 |
| 类型 | 功能 |
| 前置条件 | 任务已经历过创建 + 状态变更 + 评论 3 个动作 |

**测试步骤**：
1. 切到"历史"Tab

**预期结果**：
1. 历史列表显示 3 条记录，按时间倒序，每条含：操作人头像 + 操作描述 + 旧值/新值（状态变更类） + 时间戳

---

### TC-TASK-F-017 更新任务字段

| 字段 | 值 |
|------|-----|
| 模块 | 任务中心 |
| 优先级 | P1 |
| 类型 | 功能 |
| 前置条件 | 任务详情页打开（创建者=fde_p6_01） |

**测试步骤**：
1. 点击"编辑"按钮
2. 将优先级改为 P1，截止改为后天
3. 保存

**预期结果**：
1. 进入编辑态，所有字段可改
2. 保存后回详情态，新值生效，操作历史新增 2 条变更

---

### TC-TASK-F-018 删除任务

| 字段 | 值 |
|------|-----|
| 模块 | 任务中心 |
| 优先级 | P1 |
| 类型 | 功能 |
| 前置条件 | 任务详情页打开（创建者=fde_p6_01） |

**测试步骤**：
1. 点击"更多 → 删除"
2. 弹窗确认"删除"

**预期结果**：
1. 弹出确认弹窗，标题"确认删除任务？"，警示文案
2. 任务被软删（is_deleted=1），跳转回列表，该任务消失

---

### TC-TASK-F-019 批量勾选与批量更新状态（≤ 10 项）

| 字段 | 值 |
|------|-----|
| 模块 | 任务中心 |
| 优先级 | P1 |
| 类型 | 功能 |
| 前置条件 | 任务列表存在 ≥ 5 条 |

**测试步骤**：
1. 勾选 3 条任务前的 checkbox
2. 顶部出现批量操作栏，点击"批量改状态"，选 done

**预期结果**：
1. 批量操作栏出现，显示"已选 3 项"
2. 直接执行（不触发二次确认），3 条任务状态变 done

---

### TC-TASK-F-020 批量更新状态 > 10 项触发二次确认

| 字段 | 值 |
|------|-----|
| 模块 | 任务中心 |
| 优先级 | P0 |
| 类型 | 功能 |
| 前置条件 | fde_p7_01 登录，列表存在 ≥ 12 条任务 |

**测试步骤**：
1. 勾选 12 条任务
2. 点击"批量改状态"，选 done
3. 在 actionCard 中点"确认执行"

**预期结果**：
1. 弹出 actionCard 预览：标题"批量更新 12 项状态" + severity=high + impact list 显示 12 项 + 60s 倒计时
2. 弹窗显示"待确认"徽章
3. 12 条任务全部变 done，actionCard 状态变"已完成"

---

### TC-TASK-F-021 批量操作 actionCard 取消

| 字段 | 值 |
|------|-----|
| 模块 | 任务中心 |
| 优先级 | P1 |
| 类型 | 功能 |
| 前置条件 | actionCard 已弹出（12 项批量场景） |

**测试步骤**：
1. 在 actionCard 中点"取消"

**预期结果**：
1. actionCard 关闭，无任何任务被改动，后端 actionId 状态变 cancelled

---

### TC-TASK-F-022 批量操作 actionCard 60s 过期

| 字段 | 值 |
|------|-----|
| 模块 | 任务中心 |
| 优先级 | P1 |
| 类型 | 功能 |
| 前置条件 | actionCard 已弹出 |

**测试步骤**：
1. 等待 65 秒
2. 点"确认执行"

**预期结果**：
1. actionCard 倒计时归 0，按钮置灰，提示"已过期，请重新发起"
2. 后端返回 410 BIZ_ACTION_EXPIRED，前端 toast 错误信息

---

### TC-TASK-F-023 批量指派给其他人

| 字段 | 值 |
|------|-----|
| 模块 | 任务中心 |
| 优先级 | P2 |
| 类型 | 功能 |
| 前置条件 | fde_p7_01 登录，勾选 5 任务 |

**测试步骤**：
1. 点"批量指派"，选 fde_p5_01

**预期结果**：
1. 5 任务责任人变 fde_p5_01，每条任务历史新增一条变更

---

### TC-TASK-F-024 越权访问他人任务详情

| 字段 | 值 |
|------|-----|
| 模块 | 任务中心 |
| 优先级 | P0 |
| 类型 | 功能 |
| 前置条件 | outsider_01 登录（不属于任何项目），种子任务 T20001 由 fde_p6_01 创建 |

**测试步骤**：
1. 直接访问 `/tasks/T20001`

**预期结果**：
1. 显示 403 错误页"无权访问该任务"，并提供"返回工作台"链接

---

### TC-TASK-F-025 T 助手欢迎语自动展示

| 字段 | 值 |
|------|-----|
| 模块 | T 任务助手 |
| 优先级 | P1 |
| 类型 | 功能 |
| 前置条件 | fde_p6_01 登录任务页 |

**测试步骤**：
1. 进入任务中心，观察右侧 Copilot

**预期结果**：
1. T 助手默认展开（400px），首屏自动展示文本欢迎语，介绍 4 大能力（创建/查询/批量/工作量分析），下方有 4 个 chip 推荐问题

---

### TC-TASK-F-026 T 助手对话创建任务（actionCard）

| 字段 | 值 |
|------|-----|
| 模块 | T 任务助手 |
| 优先级 | P0 |
| 类型 | 功能 |
| 前置条件 | T 助手就绪 |

**测试步骤**：
1. 在 T 输入框输入"创建一个明天截止的 P0 任务：完成客户A的POC方案"
2. 发送
3. 在弹出的 actionCard 中点"确认执行"

**预期结果**：
1. T 流式回复，先出"已为你准备好任务预览"文本
2. 紧跟 actionCard：标题"创建任务"+ 字段对比（标题/优先级 P0/截止=明天/项目=未指定）
3. 任务创建成功，actionCard 变"已完成"，列表顶部出现新任务

---

### TC-TASK-F-027 T 助手批量延期（actionCard + impact）

| 字段 | 值 |
|------|-----|
| 模块 | T 任务助手 |
| 优先级 | P1 |
| 类型 | 功能 |
| 前置条件 | 数据库存在 3 条逾期任务（截止 < 今天） |

**测试步骤**：
1. 对 T 输入"把所有逾期任务延期到下周五"
2. 在 actionCard 中查看 impact list
3. 确认执行

**预期结果**：
1. actionCard 标题"批量延期 3 项任务"
2. impact 折叠区显示 3 条任务标题 + 旧截止/新截止
3. 3 任务截止日期变更为下周五

---

### TC-TASK-F-028 T 助手工作量分析（report 卡）

| 字段 | 值 |
|------|-----|
| 模块 | T 任务助手 |
| 优先级 | P1 |
| 类型 | 功能 |
| 前置条件 | fde_p6_01 当前有 10 任务（4 P0/3 P1/2 P2/1 P3） |

**测试步骤**：
1. 对 T 输入"分析下我的工作量"

**预期结果**：
1. T 回复 report 卡：标题"工作量分析" + 风险等级=高（红）+ 描述"P0 任务过多（4 项），建议..." + 红黄绿三色分级展示

---

### TC-TASK-F-029 T 助手感知页面筛选上下文

| 字段 | 值 |
|------|-----|
| 模块 | T 任务助手 |
| 优先级 | P2 |
| 类型 | 功能 |
| 前置条件 | 任务列表已加 "项目=阿里云A 状态=in_progress" 筛选 |

**测试步骤**：
1. T 助手 Context 区检查
2. 输入"列一下当前任务"

**预期结果**：
1. Context 区自动出现 2 个 tag："项目: 阿里云A" + "状态: in_progress"
2. T 回复仅基于筛选结果列任务，不返回全部

---

### TC-TASK-F-030 T 助手 chip 推荐问题一键发送

| 字段 | 值 |
|------|-----|
| 模块 | T 任务助手 |
| 优先级 | P2 |
| 类型 | 功能 |
| 前置条件 | T 助手已展示欢迎语 |

**测试步骤**：
1. 点击 chip "我今天有哪些 P0 任务？"

**预期结果**：
1. chip 内容自动填入输入框并发送，T 流式回复结果

---

## 二、UI/UX 用例（U · 12 条）

### TC-TASK-U-001 任务列表表格列宽自适应

| 字段 | 值 |
|------|-----|
| 模块 | 任务中心 |
| 优先级 | P2 |
| 类型 | UI |
| 前置条件 | 1440 分辨率，列表已加载 |

**测试步骤**：
1. 检查列宽

**预期结果**：
1. 标题列占主宽（≥ 40%），优先级/状态/截止/责任人列固定宽，超长标题自动 ellipsis 显示 tooltip

---

### TC-TASK-U-002 优先级 dot 颜色规范（同 DASH-U-003）

| 字段 | 值 |
|------|-----|
| 模块 | 任务中心 |
| 优先级 | P1 |
| 类型 | UI |
| 前置条件 | 列表存在 4 种优先级 |

**测试步骤**：
1. 检查 dot 颜色

**预期结果**：
1. P0 红 / P1 橙 / P2 蓝 / P3 灰，6px 直径

---

### TC-TASK-U-003 状态 tag 5 色配色

| 字段 | 值 |
|------|-----|
| 模块 | 任务中心 |
| 优先级 | P1 |
| 类型 | UI |
| 前置条件 | 列表存在 5 种状态 |

**测试步骤**：
1. 检查 5 个 tag

**预期结果**：
1. todo 灰 / in_progress 蓝 / review 紫 / done 绿 / blocked 红，文字白色或深色对比度 ≥ 4.5:1

---

### TC-TASK-U-004 行 hover 高亮

| 字段 | 值 |
|------|-----|
| 模块 | 任务中心 |
| 优先级 | P3 |
| 类型 | UI |
| 前置条件 | 列表已加载 |

**测试步骤**：
1. 鼠标悬停在某行

**预期结果**：
1. 整行背景变浅蓝（#E6F4FF），右侧出现操作图标（编辑/复制/删除）

---

### TC-TASK-U-005 批量操作栏吸顶

| 字段 | 值 |
|------|-----|
| 模块 | 任务中心 |
| 优先级 | P2 |
| 类型 | UI |
| 前置条件 | 已勾选 ≥ 1 任务 |

**测试步骤**：
1. 滚动列表

**预期结果**：
1. 批量操作栏吸顶，浮在表头之上，显示"已选 N 项 + 批量操作下拉 + 取消选择"

---

### TC-TASK-U-006 创建任务弹窗动画

| 字段 | 值 |
|------|-----|
| 模块 | 任务中心 |
| 优先级 | P3 |
| 类型 | UI |
| 前置条件 | 任务页打开 |

**测试步骤**：
1. 点击"+ 新建任务"

**预期结果**：
1. 弹窗从右侧滑入，宽度 480px，背景 mask 透明度 0.45，动画时长 300ms 缓动

---

### TC-TASK-U-007 T 助手徽章渐变（蓝）

| 字段 | 值 |
|------|-----|
| 模块 | T 任务助手 |
| 优先级 | P1 |
| 类型 | UI |
| 前置条件 | T 助手已展示 |

**测试步骤**：
1. 检查 T 助手头部徽章

**预期结果**：
1. 徽章为 32×32 圆角方块，渐变 #1677FF → #0050B3，白字 "T"，带阴影

---

### TC-TASK-U-008 actionCard 字段对比红绿色

| 字段 | 值 |
|------|-----|
| 模块 | T 任务助手 |
| 优先级 | P1 |
| 类型 | UI |
| 前置条件 | T 创建任务 actionCard 已弹出 |

**测试步骤**：
1. 检查字段对比区

**预期结果**：
1. 旧值红色 + 删除线（如无旧值则不显示），新值绿色 + 加号前缀，背景浅红/浅绿

---

### TC-TASK-U-009 actionCard 倒计时进度条

| 字段 | 值 |
|------|-----|
| 模块 | T 任务助手 |
| 优先级 | P2 |
| 类型 | UI |
| 前置条件 | actionCard 弹出 |

**测试步骤**：
1. 观察 60 秒倒计时

**预期结果**：
1. 顶部进度条从 100% 线性减少至 0%，颜色从蓝 → 黄（< 20s） → 红（< 5s）

---

### TC-TASK-U-010 T 助手消息流式渲染

| 字段 | 值 |
|------|-----|
| 模块 | T 任务助手 |
| 优先级 | P1 |
| 类型 | UI |
| 前置条件 | 对 T 发送任意问题 |

**测试步骤**：
1. 观察消息渲染

**预期结果**：
1. 文本字符逐字流式追加，光标符号"▌"在末尾闪烁，完成后光标消失

---

### TC-TASK-U-011 T 助手折叠/展开

| 字段 | 值 |
|------|-----|
| 模块 | T 任务助手 |
| 优先级 | P1 |
| 类型 | UI |
| 前置条件 | T 助手默认展开 |

**测试步骤**：
1. 点击 T 头部"折叠"图标
2. 再次点击展开

**预期结果**：
1. Copilot 区从 400px 缩到 56px，仅留徽章 + 展开图标
2. 恢复 400px，状态保留（不重置对话）

---

### TC-TASK-U-012 推荐 chip 视觉

| 字段 | 值 |
|------|-----|
| 模块 | T 任务助手 |
| 优先级 | P2 |
| 类型 | UI |
| 前置条件 | T 欢迎区已展示 |

**测试步骤**：
1. 检查 4 个 chip

**预期结果**：
1. chip 灰底圆角 16px，hover 变浅蓝边框，间距 8px，自动换行

---

## 三、接口用例（I · 13 条）

### TC-TASK-I-001 GET /tasks 主流程

| 字段 | 值 |
|------|-----|
| 模块 | 任务中心 |
| 优先级 | P0 |
| 类型 | 接口 |
| 前置条件 | fde_p6_01 token 有效 |

**测试步骤**：
1. `GET /api/v1/tasks?page=1&size=20`

**预期结果**：
1. HTTP 200，`data.items` 数组 + `data.total` + `data.page=1` + `data.size=20`

---

### TC-TASK-I-002 GET /tasks 多筛选条件

| 字段 | 值 |
|------|-----|
| 模块 | 任务中心 |
| 优先级 | P1 |
| 类型 | 接口 |
| 前置条件 | 同上 |

**测试步骤**：
1. `GET /api/v1/tasks?keyword=POC&status=todo&status=in_progress&priority=p0`

**预期结果**：
1. HTTP 200，返回交集结果，status 数组多值生效

---

### TC-TASK-I-003 POST /tasks 主流程

| 字段 | 值 |
|------|-----|
| 模块 | 任务中心 |
| 优先级 | P0 |
| 类型 | 接口 |
| 前置条件 | 同上 |

**测试步骤**：
1. `POST /api/v1/tasks` body=`{"title":"测试任务","priority":"p1","status":"todo"}`

**预期结果**：
1. HTTP 201（或 200 with code=0），返回完整 TaskDTO 含 `id` 和 `gmtCreate`

---

### TC-TASK-I-004 POST /tasks 标题为空校验

| 字段 | 值 |
|------|-----|
| 模块 | 任务中心 |
| 优先级 | P1 |
| 类型 | 接口 |
| 前置条件 | 同上 |

**测试步骤**：
1. body=`{"title":"","priority":"p1"}`
2. body=`{"title":"a"*201,"priority":"p1"}`
3. body=`{"title":"x","priority":"p9"}` 非法枚举

**预期结果**：
1. 422 BIZ_INVALID_PARAM 标题不能为空
2. 422 BIZ_INVALID_PARAM 标题超长
3. 422 BIZ_INVALID_PARAM priority 非法

---

### TC-TASK-I-005 PUT /tasks/{id} 更新

| 字段 | 值 |
|------|-----|
| 模块 | 任务中心 |
| 优先级 | P1 |
| 类型 | 接口 |
| 前置条件 | 已有任务 id=T20001（fde_p6_01 创建） |

**测试步骤**：
1. `PUT /api/v1/tasks/T20001` body=`{"status":"done"}`

**预期结果**：
1. HTTP 200，返回更新后 TaskDTO，status=done

---

### TC-TASK-I-006 DELETE /tasks/{id}

| 字段 | 值 |
|------|-----|
| 模块 | 任务中心 |
| 优先级 | P1 |
| 类型 | 接口 |
| 前置条件 | 同上 |

**测试步骤**：
1. `DELETE /api/v1/tasks/T20001`
2. 再次 `GET /api/v1/tasks/T20001`

**预期结果**：
1. HTTP 200
2. HTTP 404 BIZ_TASK_NOT_FOUND

---

### TC-TASK-I-007 POST /tasks/batch-update-status ≤10 直接执行

| 字段 | 值 |
|------|-----|
| 模块 | 任务中心 |
| 优先级 | P0 |
| 类型 | 接口 |
| 前置条件 | 5 任务存在 |

**测试步骤**：
1. body=`{"ids":[1,2,3,4,5],"status":"done"}`

**预期结果**：
1. HTTP 200，`data.updated=5`，无需 actionId

---

### TC-TASK-I-008 batch-update >10 缺 actionId 报错

| 字段 | 值 |
|------|-----|
| 模块 | 任务中心 |
| 优先级 | P0 |
| 类型 | 接口 |
| 前置条件 | 12 任务存在 |

**测试步骤**：
1. body=`{"ids":[1..12],"status":"done"}` 不带 actionId

**预期结果**：
1. HTTP 422 BIZ_ACTION_REQUIRED (8001) "高危操作需先获取 actionId"

---

### TC-TASK-I-009 batch-update 携带过期 actionId

| 字段 | 值 |
|------|-----|
| 模块 | 任务中心 |
| 优先级 | P1 |
| 类型 | 接口 |
| 前置条件 | 已 stage 一个 actionId 并等待 65s |

**测试步骤**：
1. body=`{"ids":[1..12],"status":"done","actionId":"xxx"}`

**预期结果**：
1. HTTP 410 BIZ_ACTION_EXPIRED (8003)

---

### TC-TASK-I-010 batch-update actionId 用户不匹配

| 字段 | 值 |
|------|-----|
| 模块 | 任务中心 |
| 优先级 | P1 |
| 类型 | 接口 |
| 前置条件 | fde_p6_01 stage actionId，fde_p7_01 尝试执行 |

**测试步骤**：
1. fde_p7_01 调 batch-update 携带 fde_p6_01 的 actionId

**预期结果**：
1. HTTP 403 BIZ_ACTION_USER_MISMATCH (8004)

---

### TC-TASK-I-011 越权访问他人任务返回 403

| 字段 | 值 |
|------|-----|
| 模块 | 任务中心 |
| 优先级 | P0 |
| 类型 | 接口 |
| 前置条件 | outsider_01 token，T20001 属于 fde_p6_01 |

**测试步骤**：
1. `GET /api/v1/tasks/T20001` Authorization=outsider_01

**预期结果**：
1. HTTP 403 BIZ_NO_TASK_PERMISSION (3002)

---

### TC-TASK-I-012 POST /tasks/{id}/comments 添加评论

| 字段 | 值 |
|------|-----|
| 模块 | 任务中心 |
| 优先级 | P2 |
| 类型 | 接口 |
| 前置条件 | T20001 存在 |

**测试步骤**：
1. body=`{"content":"完成 50%"}`

**预期结果**：
1. HTTP 200，返回 Comment 对象 含 id/作者/内容/创建时间

---

### TC-TASK-I-013 GET /tasks/{id}/history 返回历史

| 字段 | 值 |
|------|-----|
| 模块 | 任务中心 |
| 优先级 | P2 |
| 类型 | 接口 |
| 前置条件 | T20001 已经历过状态变更 + 评论 |

**测试步骤**：
1. `GET /api/v1/tasks/T20001/history`

**预期结果**：
1. HTTP 200，返回 History 数组，按时间倒序，含 op 类型/操作人/before/after

---

**文档结束** · 共 55 条用例（功能 30 + UI 12 + 接口 13）
