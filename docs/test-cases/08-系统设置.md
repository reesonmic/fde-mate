# 08 · 系统设置 测试用例

> 编写依据：[PRD §5.8 / §7.4](../FDE工作台产品需求文档.md) + [02-后端详细设计.md §2.1 §2.10](../detail-design/02-后端详细设计.md)
> 编写规范：见 [00-通用规范.md](./00-通用规范.md)

| 项目 | 信息 |
|------|------|
| 模块 | 系统设置 |
| 模块代号 | SET |
| 用例总数 | 25（功能 15 + UI 4 + 接口 6） |
| 优先级分布 | P0:5 / P1:11 / P2:7 / P3:2 |

---

## 一、功能用例（F · 15 条）

### TC-SET-F-001 个人偏好正常加载（主流程）

| 字段 | 值 |
|------|-----|
| 模块 | 系统设置 |
| 优先级 | P0 |
| 类型 | 功能 |
| 前置条件 | fde_p6_01 已登录 |

**测试步骤**：
1. 点击右上角头像 → "设置"
2. 默认进入"个人偏好"

**预期结果**：
1. 跳转 `/settings/profile`，左侧 4 个 tab（个人偏好/通知配置/AI 模型/账号安全）
2. 表单回填当前用户：昵称/邮箱/头像/部门

---

### TC-SET-F-002 修改昵称

| 字段 | 值 |
|------|-----|
| 模块 | 系统设置 |
| 优先级 | P1 |
| 类型 | 功能 |
| 前置条件 | 个人偏好页 |

**测试步骤**：
1. 修改昵称为"张三-FDE"
2. 点"保存"

**预期结果**：
1. HTTP 200，toast"保存成功"，顶部导航昵称同步刷新

---

### TC-SET-F-003 上传头像

| 字段 | 值 |
|------|-----|
| 模块 | 系统设置 |
| 优先级 | P2 |
| 类型 | 功能 |
| 前置条件 | 个人偏好页 |

**测试步骤**：
1. 点击头像，选择 200KB jpg
2. 裁剪并确认

**预期结果**：
1. 头像预览刷新，HTTP 200，全局头像同步

---

### TC-SET-F-004 头像超大文件拒绝

| 字段 | 值 |
|------|-----|
| 模块 | 系统设置 |
| 优先级 | P3 |
| 类型 | 功能 |
| 前置条件 | 准备 6MB 图片 |

**测试步骤**：
1. 上传

**预期结果**：
1. toast"图片不得超过 5MB"

---

### TC-SET-F-005 通知配置三通道

| 字段 | 值 |
|------|-----|
| 模块 | 系统设置 |
| 优先级 | P1 |
| 类型 | 功能 |
| 前置条件 | 切到"通知配置" |

**测试步骤**：
1. 检查 3 通道开关：钉钉 / 邮件 / 站内信

**预期结果**：
1. 3 通道默认全开，每通道下方有 5 类事件子开关（任务指派/项目风险/客户健康度/周报到达/系统公告）

---

### TC-SET-F-006 关闭某通道

| 字段 | 值 |
|------|-----|
| 模块 | 系统设置 |
| 优先级 | P1 |
| 类型 | 功能 |
| 前置条件 | 通知配置页 |

**测试步骤**：
1. 关闭"邮件"主开关

**预期结果**：
1. 5 子开关同步置灰禁用，HTTP 200 自动保存

---

### TC-SET-F-007 触发通知（端到端）

| 字段 | 值 |
|------|-----|
| 模块 | 系统设置 |
| 优先级 | P2 |
| 类型 | 功能 |
| 前置条件 | 仅开"站内信"，关其他 |

**测试步骤**：
1. 由 fde_p7_01 给 fde_p6_01 指派任务
2. 等 30 秒

**预期结果**：
1. fde_p6_01 仅收站内信红点，钉钉/邮件无消息

---

### TC-SET-F-008 AI 模型选择

| 字段 | 值 |
|------|-----|
| 模块 | 系统设置 |
| 优先级 | P1 |
| 类型 | 功能 |
| 前置条件 | 切到"AI 模型" |

**测试步骤**：
1. 检查模型列表
2. 选择"通义千问 max"，保存

**预期结果**：
1. 显示 3 模型：GPT-4 / Claude 3.5 / 通义千问 max（默认勾选 GPT-4）
2. HTTP 200，下次 AI 对话使用通义千问

---

### TC-SET-F-009 模型切换全局生效

| 字段 | 值 |
|------|-----|
| 模块 | 系统设置 |
| 优先级 | P2 |
| 类型 | 功能 |
| 前置条件 | 已切换模型 |

**测试步骤**：
1. 进入 AI 对话页发送消息
2. 进入任务页打开 T 助手对话

**预期结果**：
1. 两处都使用通义千问（响应风格区别于 GPT-4），后端日志记录 model=qwen-max

---

### TC-SET-F-010 修改密码（主流程）

| 字段 | 值 |
|------|-----|
| 模块 | 系统设置 |
| 优先级 | P0 |
| 类型 | 功能 |
| 前置条件 | 切到"账号安全" |

**测试步骤**：
1. 输入旧密码 + 新密码 + 确认新密码
2. 点"修改"

**预期结果**：
1. HTTP 200，toast"密码已更新，请重新登录"，自动登出

---

### TC-SET-F-011 旧密码错误

| 字段 | 值 |
|------|-----|
| 模块 | 系统设置 |
| 优先级 | P0 |
| 类型 | 功能 |
| 前置条件 | 同上 |

**测试步骤**：
1. 旧密码故意填错
2. 提交

**预期结果**：
1. HTTP 400 BIZ_OLD_PWD_INVALID (1003)，提示"旧密码错误"

---

### TC-SET-F-012 新密码强度不达标

| 字段 | 值 |
|------|-----|
| 模块 | 系统设置 |
| 优先级 | P1 |
| 类型 | 功能 |
| 前置条件 | 同上 |

**测试步骤**：
1. 新密码填"12345"
2. 提交

**预期结果**：
1. 前端拦截，提示"密码至少 8 位，含字母+数字+特殊字符"

---

### TC-SET-F-013 两次密码不一致

| 字段 | 值 |
|------|-----|
| 模块 | 系统设置 |
| 优先级 | P1 |
| 类型 | 功能 |
| 前置条件 | 同上 |

**测试步骤**：
1. 新密码与确认密码不同

**预期结果**：
1. 前端拦截，提示"两次密码不一致"

---

### TC-SET-F-014 登出

| 字段 | 值 |
|------|-----|
| 模块 | 系统设置 |
| 优先级 | P0 |
| 类型 | 功能 |
| 前置条件 | 已登录 |

**测试步骤**：
1. 点击右上角头像 → "退出登录"

**预期结果**：
1. 跳转登录页，本地 token 清空，再次访问任意页面 302 → /login

---

### TC-SET-F-015 Token 过期自动刷新

| 字段 | 值 |
|------|-----|
| 模块 | 系统设置 |
| 优先级 | P1 |
| 类型 | 功能 |
| 前置条件 | accessToken 即将过期（剩 30 秒） |

**测试步骤**：
1. 触发任意接口调用

**预期结果**：
1. 前端检测过期，使用 refreshToken 静默刷新，用户无感知，原请求自动重试成功

---

## 二、UI/UX 用例（U · 4 条）

### TC-SET-U-001 设置页布局

| 字段 | 值 |
|------|-----|
| 模块 | 系统设置 |
| 优先级 | P1 |
| 类型 | UI |
| 前置条件 | 设置页打开 |

**测试步骤**：
1. 检查布局

**预期结果**：
1. 左侧 200px 4-tab 列表，右侧自适应表单区

---

### TC-SET-U-002 选中 tab 样式

| 字段 | 值 |
|------|-----|
| 模块 | 系统设置 |
| 优先级 | P2 |
| 类型 | UI |
| 前置条件 | 切到"通知配置" |

**测试步骤**：
1. 检查 tab 样式

**预期结果**：
1. 选中 tab 蓝色文字 + 左侧 3px 蓝 indicator + 浅蓝背景

---

### TC-SET-U-003 开关组件视觉

| 字段 | 值 |
|------|-----|
| 模块 | 系统设置 |
| 优先级 | P3 |
| 类型 | UI |
| 前置条件 | 通知配置页 |

**测试步骤**：
1. 检查开关 ON / OFF 状态

**预期结果**：
1. ON=蓝底白圆，OFF=灰底白圆，过渡动画 200ms

---

### TC-SET-U-004 模型卡选中态

| 字段 | 值 |
|------|-----|
| 模块 | 系统设置 |
| 优先级 | P2 |
| 类型 | UI |
| 前置条件 | AI 模型页 |

**测试步骤**：
1. 检查 3 模型卡

**预期结果**：
1. 选中模型 2px 蓝边框 + 右上 ✓ 角标，未选中淡灰边框

---

## 三、接口用例（I · 6 条）

### TC-SET-I-001 GET /me 当前用户

| 字段 | 值 |
|------|-----|
| 模块 | 系统设置 |
| 优先级 | P1 |
| 类型 | 接口 |
| 前置条件 | fde_p6_01 token |

**测试步骤**：
1. `GET /api/v1/me`

**预期结果**：
1. HTTP 200，返回 UserDTO（昵称/邮箱/头像/角色/部门）

---

### TC-SET-I-002 PUT /me 更新个人信息

| 字段 | 值 |
|------|-----|
| 模块 | 系统设置 |
| 优先级 | P1 |
| 类型 | 接口 |
| 前置条件 | 同上 |

**测试步骤**：
1. body=`{"nickname":"张三-FDE","avatarUrl":"https://..."}`

**预期结果**：
1. HTTP 200，返回更新后 UserDTO

---

### TC-SET-I-003 PUT /me/password 修改密码

| 字段 | 值 |
|------|-----|
| 模块 | 系统设置 |
| 优先级 | P0 |
| 类型 | 接口 |
| 前置条件 | 同上 |

**测试步骤**：
1. body=`{"oldPassword":"xxx","newPassword":"Yyy@123456"}`

**预期结果**：
1. HTTP 200，旧 token 立即失效

---

### TC-SET-I-004 PUT /me/notifications 通知配置

| 字段 | 值 |
|------|-----|
| 模块 | 系统设置 |
| 优先级 | P1 |
| 类型 | 接口 |
| 前置条件 | 同上 |

**测试步骤**：
1. body=`{"dingtalk":true,"email":false,"inbox":true,"events":{"taskAssign":true,...}}`

**预期结果**：
1. HTTP 200

---

### TC-SET-I-005 POST /auth/refresh 刷新 token

| 字段 | 值 |
|------|-----|
| 模块 | 系统设置 |
| 优先级 | P1 |
| 类型 | 接口 |
| 前置条件 | refreshToken 有效 |

**测试步骤**：
1. body=`{"refreshToken":"xxx"}`

**预期结果**：
1. HTTP 200，返回新 accessToken

---

### TC-SET-I-006 POST /auth/logout 登出

| 字段 | 值 |
|------|-----|
| 模块 | 系统设置 |
| 优先级 | P0 |
| 类型 | 接口 |
| 前置条件 | accessToken 有效 |

**测试步骤**：
1. `POST /api/v1/auth/logout`

**预期结果**：
1. HTTP 200；用旧 token 再调任意接口返回 401 BIZ_TOKEN_INVALID

---

**文档结束** · 共 25 条用例（功能 15 + UI 4 + 接口 6）
